// this file configuration can be used in case you are loading exactly `reactivemongo-getstarted` and nothing else.

name := "reactivemongo-getstarted"

version := "0.1"

scalaVersion := "2.13.0"

val reactiveMongo = "org.reactivemongo" %% "reactivemongo" % "0.18.6"

lazy val `reactivemongo-getstarted` = (project in file("."))
  .settings(libraryDependencies ++= {
    Seq(reactiveMongo)
  })
